apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shortlink-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "shortlink.test.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: shortlink-app
            port:
              number: 8000
