apiVersion: v1
kind: Service
metadata:
  name: {{ include "shortlink-app.fullname" . }}
  labels:
    {{- include "shortlink-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shortlink-app.selectorLabels" . | nindent 4 }}
